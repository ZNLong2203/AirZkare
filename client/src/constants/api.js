const BE = "http://localhost:2222/api";

const API = {
    LOGIN: `${BE}/auth/login`,
    REGISTER: `${BE}/auth/register`,
    PASSENGER: `${BE}/passenger`,
    AIRPORT: `${BE}/airport`,
}

export default API;
